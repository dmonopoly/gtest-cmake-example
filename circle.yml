machine:
  services:
    - docker
  ruby:
    - 2.2.2

dependencies:
  override:
    - docker build -t test .
test:
  override:
    - docker run test /bin/sh -c 'cd build; make test'
    - bundle
    - bundle exec rspec