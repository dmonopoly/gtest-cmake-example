machine:
  services:
    - docker

dependencies:
  pre:
    - mkdir build; cd build; cmake -Dtest=ON .. 
  override:
    - cd build; make
test:
  override:
    - docker run yutakakinjyo/gtest /bin/sh -c 'cd build && make test'