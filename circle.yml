machine:
  services:
    - docker
  ruby:
    version: 2.2.2

dependencies:
  override:
    - docker build -t test .
test:
  override:
    - docker run --rm -v ${PWD}:/project/repo -it test /bin/sh -c 'cd repo; cmake -P build.cmake; cd build; ./runUnitTests'
    - bundle
    - bundle exec rspec